init_config:

instances:
  # No configuration is needed for this check. However, you can add tags.
  # A single instance needs to be defined with any value.
  - tags:
      app: kafka
      owner: ops
